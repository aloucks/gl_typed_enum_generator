# GL Typed Enum Generator

Create OpenGL bindings with typed enums.

Currently depends on a fork of [gl_generator](https://github.com/aloucks/gl-rs/tree/enum_groups) that provides enum group information.
